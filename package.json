{
  "name": "dashboard-automatas-ii-root",
  "version": "1.0.0",
  "description": "Orquestador para despliegue en Render",
  "private": true,
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm install --prefix Sensores",
    "start": "USE_CLOUD_DB=true concurrently \"npm run start:backend\" \"npm run start:frontend\" \"npm run start:sensores\"",
    "start:backend": "npm run dev --prefix backend",
    "start:frontend": "npm run dev --prefix frontend",
    "start:sensores": "npm run all --prefix Sensores"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}